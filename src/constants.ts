/** Default maximum number of visible entries */
export const DEFAULT_MAX_HEIGHT = 10;

/** Default starting path */
export const DEFAULT_INITIAL_PATH = process.cwd();

/** Spinner frames for loading indicator */
export const SPINNER_FRAMES = ['⠋', '⠙', '⠹', '⠸', '⠼', '⠴', '⠦', '⠧', '⠇', '⠏'] as const;

/** Spinner frame interval in ms */
export const SPINNER_INTERVAL = 80;
